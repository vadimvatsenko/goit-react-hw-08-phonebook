import Box from '@mui/material/Box';

export default function Footer() {
  return (
    <Box
      sx={{
        width: '100%',
        height: 50,
        backgroundColor: 'primary.main',
      }}
    />
  );
}